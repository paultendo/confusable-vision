/**
 * discover-novel.ts -- Milestone 2 (stub)
 *
 * Discover confusable pairs NOT listed in TR39 by rendering identifier-safe
 * Unicode characters and comparing them against Latin targets.
 *
 * TODO:
 * - Enumerate identifier-safe Unicode characters (UAX #31)
 * - Render each against common Latin letters/digits
 * - Threshold SSIM/pHash similarity to flag potential confusables
 * - Cross-reference with dnstwist homoglyph lists
 * - Consider multi-character confusables (e.g. "rn" vs "m")
 * - Output novel-confusables.json
 */

console.log('discover-novel: not yet implemented (milestone 2)');
process.exit(0);
