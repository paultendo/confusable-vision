/**
 * score-all-pairs.ts -- Milestone 1b (stub)
 *
 * Expand analysis to full confusables.txt (~4,500 entries).
 * Produce a weighted confusable map with font-aware confidence scores.
 *
 * TODO:
 * - Parse Unicode confusables.txt into structured pairs
 * - Re-use render/normalise/compare pipeline from milestone 1
 * - Aggregate scores across fonts into a weighted confusable map
 * - Output confusable-scores.json with per-pair confidence values
 */

console.log('score-all-pairs: not yet implemented (milestone 1b)');
process.exit(0);
